services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cram-web
    restart: unless-stopped

    ports:
      - "6767:3000"

    env_file:
      - .env

    environment:
      NODE_ENV: production